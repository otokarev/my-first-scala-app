define([ 'underscore' ], function(app) {
  'use strict';

  var ChannelSubscribersCtrl = function($scope) {

  };
  ChannelSubscribersCtrl.$inject = [ '$scope' ];

  return  ChannelSubscribersCtrl;
});

