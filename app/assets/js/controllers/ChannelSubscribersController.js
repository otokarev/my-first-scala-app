define([ 'underscore' ], function(app) {
  'use strict';

  var ChannelSubscribersCtrl = function($scope, playRoutes) {

  };
  ChannelSubscribersCtrl.$inject = [ '$scope', 'playRoutes' ];

  return  ChannelSubscribersCtrl;
});

